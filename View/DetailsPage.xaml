<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             
             xmlns:viewModel="clr-namespace:MauiApp1.ViewModel"
             x:Class="MauiApp1.View.DetailsPage"
             x:DataType="viewModel:DetailsViewModel"
             Title="DetailsPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Text="Minimize" BackgroundColor="LightGray" Command="{Binding ToggleSidebarCommand}" />
        <VerticalStackLayout x:Name="Sidebar" Grid.Row="1" BackgroundColor="LightGray" Padding="10" WidthRequest="200">
            <Button Text="Home" Command="{Binding NavigateCommand}" CommandParameter="HomePage" />
            <Button Text="Details" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>